services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: worthflow-backend:dev
    container_name: worthflow-backend-dev
    env_file:
      - ./backend/.env
    ports:
      - "8000:8000"
    volumes:
      - ./backend/app:/app/app
